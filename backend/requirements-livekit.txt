# Voice worker (LiveKit Agents) + browser JWT: ``pip install -r requirements-livekit.txt``
#
# Run from ``backend/``: ``PYTHONPATH=. python scripts/run_voice_worker.py``
# Needs FastAPI on ``VOICE_API_BASE`` (TTS/tools), Ollama, and LiveKit server credentials.

# livekit-api is pulled in by livekit-agents (>=1.0.7,<2) and satisfies FastAPI ``GET /livekit/token``.
livekit-agents[silero,openai]>=1.4.0,<2.0
livekit>=1.1.0,<2.0
