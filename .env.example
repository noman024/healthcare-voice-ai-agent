# Monorepo — no secrets in this repo root file.
#
# Templates (copy targets are gitignored when they contain secrets):
#
#   cp backend/.env.example backend/.env
#   cp frontend/.env.local.example frontend/.env.local
#
# Read the banner at the top of each template for rotation and NEXT_PUBLIC_* safety.
